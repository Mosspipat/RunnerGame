using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class codeSystem : MonoBehaviour {


    //  Check edge version

    /*void CheckEdge()
    {
        if (this.transform.position.x <= edgeLeft.position.x)
        {
            float posXLeft = edgeLeft.position.x;
            this.transform.position = new Vector3(posXLeft,this.transform.position.y,this.transform.position.z);
            Debug.Log("out Left");
            /*TerrianEnvironment.transform.position = new Vector3(0,20,0);*/                        //Tranform Terrain move set
    /* }

        if (this.transform.position.x >= edgeRight.position.x)
        {
            float posXRight = edgeRight.position.x;
            this.transform.position = new Vector3(posXRight,this.transform.position.y,this.transform.position.z);
            Debug.Log("out Right");
        }*/
   /* }*/

}
